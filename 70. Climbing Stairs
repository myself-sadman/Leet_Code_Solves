class Solution {
public:
    int climbStairs(int n) {
        if(n<=2) {
            return n;
        }
        int st1=1;
        int st2=2;
        for(int i=3;i<=n;i++){
            int sum=st1+st2;
            st1=st2;
            st2=sum;
        }
        return st2;
    }
};
